export const passwordConstraint = "must have 8 characters including at least 1 uppercase, 1 lowercase and 1 special character including numbers";

export const hasMinLengthConstraint = (minLength) => `must hold a value with length greater or equal to ${minLength}`;

export const emailConstraint = "must be in email format";
